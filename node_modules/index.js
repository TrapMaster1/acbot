// bot.js


const Twit = require('twit');
const axios = require('axios');

const TH = new Twit({
    consumer_key: process.env.TWITTER_API_CONSUMER,
    consumer_secret: process.env.TWITTER_API_CONSUMER_SECRET,
    access_token: process.env.TWITTER_API_ACCESS,
    access_token_secret: process.env.TWITTER_API_ACCESS_SECRET,
});

const _self = {
    id: 1045101008433664005,
    screen_name: 'Bot Boi'
};


const Birthdays = {

    villagers = []

}

function getBirthdays () {
    axios.get('https://animalcrossing.fandom.com/api/v1/Articles/AsSimpleJson?id=1')
    .then(res => {
        console.log(res);
    })
}

getBirthdays();
return;
